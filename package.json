{
  "name": "sstv3",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run --prefix packages/core lint && npm run --prefix packages/frontend lint && npm run --prefix packages/functions lint && npm run --prefix packages/scripts lint"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22",
    "prettier": "^3.5.1",
    "typescript": "^5"
  },
  "dependencies": {
    "sst": "^3"
  }
}
